// ── EDIT THESE THREE LINES, then everything else just works ──────────────
window.MAP_CONFIG = {
  owner:  "YOUR_GITHUB_USERNAME",   // e.g. "jsmith"
  repo:   "YOUR_REPO_NAME",         // e.g. "my-game-map"
  branch: "main",                   // the protected branch markers merge into
};
// ───────────────────────────────────────────────────────────────────────
